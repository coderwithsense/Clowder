import { CatsProps } from '../types/cats'

export const getOwnedCATs = async (): Promise<CatsProps[]> => {
    return [];
}

export const deployCAT = async (formData: DeployContractProps): Promise<string> => {
    return '';
}